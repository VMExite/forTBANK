spring:
  datasource:
    url: jdbc:tc:postgresql:16:///testdb
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true
    change-log: classpath:/migrations/master.yaml
    url: jdbc:tc:postgresql:16:///testdb
    user: test
    password: test

app:
  telegram:
    url: http://test.url
    token: test-token
    update-listener-sleep: 1s
    debug: true
  kafka:
    link-update-topic: linkUpdateEvent
    link-update-topic-dlt: linkUpdateEvent-dlt
    retry-interval: 100
    retry-max-attempts: 1
